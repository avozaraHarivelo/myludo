<?php

namespace App\Utils;

class Utils
{

    // public static function filter_game(array $jouets)
    // {
    //     return array_filter($lessons, function($l) use ($courses) {
    //         return !in_array($l->getCourse(), $courses);
    //     });
    // }

    // public static function in_course_lesson(array $lessons, array $courses)
    // {
    //     return array_filter($lessons, function($l) use ($courses) {
    //         return in_array($l->getCourse(), $courses);
    //     });
    // }
}
